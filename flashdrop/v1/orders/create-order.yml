info:
  name: create-order
  type: http
  seq: 2

http:
  method: POST
  url: "{{versioned_url}}/orders"
  body:
    type: json
    data: |-
      {
        "productSKU":"IPHONE-17-PRO-MAX-256-BLK",
        "flashSaleId":"699a7960fa696ce129c87785"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
