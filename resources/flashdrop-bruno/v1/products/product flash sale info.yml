info:
  name: product flash sale info
  type: http
  seq: 3

http:
  method: GET
  url: "{{versioned_url}}/products/:sku/sale-status"
  params:
    - name: sku
      value: IPHONE-17-PRO-MAX-256-BLK
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
