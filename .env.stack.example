# Copy to .env.stack and set for production.
# Used by: docker stack deploy and scripts/deploy-stack.sh

# API image (required for deploy). Use a registry for multi-node: myreg.io/flashdrop/api:1.0.0
API_IMAGE=flashdrop/api:latest

# Optional overrides (defaults are in docker-stack.yml)
# DATABASE_URL=mongodb://mongodb:27017/flashdrop
# REDIS_URL=redis://redis:6379
# CORS_ORIGIN=https://app.example.com
# LOG_LEVEL=info
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=100

# Grafana (change in production)
# GF_ADMIN_USER=admin
# GF_ADMIN_PASSWORD=changeme
# GF_SERVER_ROOT_URL=https://grafana.example.com
